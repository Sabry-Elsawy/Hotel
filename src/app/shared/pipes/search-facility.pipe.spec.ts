import { SearchFacilityPipe } from './search-facility.pipe';

describe('SearchFacilityPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFacilityPipe();
    expect(pipe).toBeTruthy();
  });
});
