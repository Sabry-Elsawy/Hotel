import { SearchAdsPipe } from './search-ads.pipe';

describe('SearchAdsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAdsPipe();
    expect(pipe).toBeTruthy();
  });
});
